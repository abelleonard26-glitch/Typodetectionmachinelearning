# Dependensi untuk proyek Deteksi & Koreksi Typo (Traditional ML)
# Instal dengan:  pip install -r requirements.txt

# --- Inti aplikasi web (dibutuhkan saat deploy ke Streamlit) ---
streamlit>=1.30
# scikit-learn DIPIN PERSIS ke versi pembuat model agar artefak .joblib
# (di folder models/) dapat dimuat tanpa peringatan versi & tanpa risiko.
scikit-learn==1.7.2
numpy>=1.24
scipy>=1.10
joblib>=1.3
xgboost>=2.0

# --- Hanya untuk training/EDA di notebook (boleh dihapus untuk build cloud lebih ramping) ---
pandas>=2.0
matplotlib>=3.7
notebook>=7.0
jupyter>=1.0
